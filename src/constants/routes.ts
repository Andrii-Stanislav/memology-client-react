export enum ROUTES {
  // * auth
  LOGIN = '/login',
  SIGN_UP = '/sign-up',

  // * private
  HOME = '/',
  GAMES = '/games',
  MEMES = '/memes',
  SITUATIONS = '/situations',
  GAME_PAGE = '/games/:gameId',
}

import { Container, Stack, Typography } from '@mui/material';

export const Home = () => {
  return (
    <Container component="main">
      <Stack alignItems="center" spacing={4}>
        <Typography variant="h3">Here will be home page :)</Typography>
      </Stack>
    </Container>
  );
};
